import { StyleSheet } from "react-native";

export const MenuStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    optionsContainer: {
        flexGrow: 0,
        marginTop: 10,
        marginBottom: 10,
    },
    options: {
        paddingHorizontal: 10,
        paddingVertical: 10,
    }
});